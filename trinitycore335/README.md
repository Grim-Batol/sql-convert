# trinitycore335 target

Reserved for the **TrinityCore 3.3.5** target. The query layout is the same
as `azerothcore/` — `NN_<table>.sql`, with an optional `NN_<table>.preamble.sql`.

Empty for now: the TrinityCore 3.3.5 `creature_template` / `creature` column
set differs from AzerothCore's, so each query is written per target.
